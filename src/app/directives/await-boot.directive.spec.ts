import { AwaitBootDirective } from './await-boot.directive';

describe('AwaitBootDirective', () => {
  it('should create an instance', () => {
    const directive = new AwaitBootDirective();
    expect(directive).toBeTruthy();
  });
});
